# Todo
- frameDelay:
        if (sim.frameDelay) {
            const delay = Date.now() - this._lastFrame;
            if (sim.frameDelay > delay) {
                await new Promise(r => setTimeout(r, sim.frameDelay - delay));
            }
            this._lastFrame = Date.now();
        }
- ship
- new samples in readme
- more docs/examples
    - pac-man
    - elevators
    - robot/warehouse (3d)
- react samples
- more tests
- strips
    - length, max speed, vehicle list (with positions: extends queue)
    - multi-segment
    - multi-lane
    - rail (one lane, one direction)
    - road (multi-lane, one direction)
    - canals?
